import React, { Component, Fragment } from 'react';
import { Button, Form, Table, Input, Icon, Select, Row, Col, DatePicker } from 'antd';
import moment from 'monent';
import './index.less';

const { RangePicker } = DatePicker;
const { Option } = Select;
const FormItem = Form.Item;

class <%= pageName %> extends Component {
    constructor(props) {
        super(props);
        this.onSubmit = this.onSubmit.bind(this);
    }
    onSubmit(e) {
        e.preventDefault();
        this.props.form.validateFields((err, values) => {
            if (!err) {
                // add code here...
            }
        });
    }
    getColDisplay(i) {
        const displayStyle = { display: 'block' };
        if (Object.hasOwnProperty.call(this.state, 'expand')) {
            const { expand, expandCount } = this.state;
            if (!expand && (i+1) > expandCount) {
                displayStyle.display = 'none';
            }
        }
        return displayStyle;
    }
    render() {
        const { form } = this.props;
        const { getFieldDecorator } = form;
        return (
            <div className="page-container">
<% layoutConfig.forEach(function(item){ -%><% if (item.type === 'FormContainer') { -%><%- include('./form', {configs: item.configs, getInitialValue, renderItemComponent, renderButton }); -%><% } %>
        <% if (item.type === 'TableContainer') { %><%- include('./table', {configs: item.configs}); %><%_ } -%><% }); %>
            </div>
        );
    }
}

export default <%= pageName %>;