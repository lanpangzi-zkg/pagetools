import React, { Component, Fragment } from 'react';
<%- importString %>

class <%= pageName %> extends Component {
    constructor(props) {
        super(props);
        <%- renderConstructor(extraConfig) %>
    }
    <%- renderMethod(extraConfig, fileConfig, pageName) %>
    render() {<% -%>
        <%- renderProp(extraConfig) %>
        return (
            <div className="fulu-container">
                <%- renderEnter(fileConfig, pageName, true) %>
            </div>
        );
    }
}
<%- renderExport(extraConfig) %>